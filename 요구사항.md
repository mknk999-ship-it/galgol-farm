# 갈골농원 공식 홈페이지 제작

## 0. 참고 파일

같은 폴더의 `galgol-preview-mobile.html` 은 확정된 디자인 시안이다.
**먼저 열어서 전체 구조와 스타일을 확인한 뒤 작업을 시작한다.**
아래 명세와 시안 파일이 어긋나면 시안 파일을 기준으로 한다.

`photos/` 폴더의 사진은 번호로 저장되어 있다. **5번 항목의 대응표**를 보고
`public/images/` 로 옮기면서 이름을 바꾸고 WebP로 최적화한다.

---

## 1. 개요

경상북도 영양군 청기면에서 고추를 직접 재배·세척·건조하는 **갈골농원**의 공식 홈페이지.
목적은 상품 판매가 아니라 "실제 농가가 직접 키우고 말린다"는 신뢰 전달이다.

흐름: 신뢰 → 이해 → 전화 문의

- 회원가입·장바구니·결제·주문관리·데이터베이스를 만들지 않는다. Supabase도 쓰지 않는다.
- 현재 주문은 전화로만 받는다. 스마트스토어는 추후 연결.
- **모바일 우선**으로 만든다. PC는 확장판이다.

---

## 2. 절대 규칙

1. 제공되지 않은 정보(농사 경력, 농장 규모, 농법, 매운 정도, 배송비 등)를 **지어내지 않는다.** 없으면 그 문장을 쓰지 않는다.
2. **계좌번호를 사이트에 넣지 않는다.** 입금 계좌는 전화로만 안내한다.
3. 실제 제공된 농장 사진만 쓴다. 스톡 이미지·AI 생성 이미지 금지.
4. 과장 광고나 검증되지 않은 건강 효능 표현 금지.
5. 정보가 확정되지 않은 상품에 임의의 가격을 넣지 않는다.

---

## 3. 기술 스택

- Next.js (App Router) + TypeScript + Tailwind CSS
- GitHub → Vercel 자동 배포
- 이미지는 next/image, `/public/images/` 에서 관리
- 외부 라이브러리 최소화. 애니메이션 라이브러리 금지 (IntersectionObserver로 직접 구현)
- 별도 CSS 파일 남발 금지, Tailwind 유틸리티 사용

---

## 4. 디자인 토큰 (이 값 그대로)

### 색상

```
--bg       #FFFFFF   기본 배경
--soft     #F7F5F0   교차 섹션 배경
--green    #2C4531   딥그린 밴드 (숫자 섹션, 공정 마지막 칸)
--ivory    #F4F1E9   딥그린 위 원형 배경
--deep     #232320   푸터 배경
--ink      #1A1A18   본문
--sub      #6E6862   보조 텍스트
--faint    #9A938B   흐린 라벨
--red      #B7332B   포인트 (버튼·번호·단위·라벨)
--red-deep #8E241E   버튼 active
--red-soft rgba(183,51,43,.09)   라벨 배경
--line     rgba(26,26,24,.09)
```

빨강은 버튼·번호·라벨·단위에만 쓴다. 넓은 면적에 쓰지 않는다.
사진 자체가 강렬한 빨강이라 UI까지 빨개지면 사진이 죽는다.

### 타이포그래피

- **Pretendard 하나만 사용한다.** 명조·세리프·손글씨 금지.
- CDN: `https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/variable/pretendardvariable-dynamic-subset.min.css`
- 본문: 모바일 16.5px / PC 17px, line-height 1.8, letter-spacing -0.015em
- 제목 h1~h3: **font-weight 800**, letter-spacing **-0.042em**, line-height 1.32
- Hero h1: 모바일 33px / PC 54px, letter-spacing -0.04em, line-height 1.24
- 섹션 제목 h2: 모바일 27px / PC 38px
- **작은 라벨과 큰 숫자에는 line-height를 반드시 개별 지정한다.**
  본문 1.8을 상속하면 글자 사이가 비정상적으로 벌어진다.
- 숫자에는 `font-variant-numeric: tabular-nums`

### 레이아웃

- 컨테이너: 모바일 max 600px / padding 22px, PC max 1080px / padding 40px
- 글이 많은 블록은 `.narrow` (PC max 660px)
- 섹션 상하 여백: 모바일 76px, PC 120px
- 모서리: 큰 카드 18px, 작은 카드 14px, 버튼 12px
- 버튼 최소 높이 54px (상품 카드 버튼만 40px)
- body 하단 padding 76px (모바일 고정바 자리), PC는 0

### 모션

- 스크롤 시 Fade Up 하나만. IntersectionObserver, threshold 0.1
- 숫자 카운트업 (0 → 목표값, 900ms, ease-out cubic)
- `prefers-reduced-motion: reduce` 시 모두 비활성화
- `:focus-visible` 표시 유지

### 강조 라벨 (사이트 전체 통일)

```
font-size 11~12px / weight 700 / color: --red
background: --red-soft / radius 4px / padding 3px 8px
```

**강조할 때 요소를 새로 추가하지 않는다.** 줄이 늘어나 옆 카드와 정렬이 어긋난다.
이미 있는 요소의 색·테두리를 바꾸거나, 같은 줄에 인라인으로 붙인다.

---

## 5. 이미지

`photos/` 폴더에 **1 ~ 14** 번호로 저장되어 있다.
아래 대응표대로 `public/images/` 로 옮기면서 이름을 바꾸고 WebP로 변환한다.
확장자는 jpg / png / webp 가 섞여 있을 수 있으니 실제 파일을 확인하고 처리한다.

| 원본 | 내용 | 새 파일명 | 사용처 |
|---|---|---|---|
| 1 | 산 아래 넓은 고추밭 전경 (가로) | `hero-farm` | Hero 배경, `object-position: center 58%` |
| 2 | 고추나무에 달린 붉은 고추 (가로) | `pepper-plant` | 공정 01 재배 |
| 10 | 붉은 고추와 풋고추가 함께 달린 밭 근접 (가로) | `harvest` | 공정 02 수확 |
| 8 | 세척한 고추를 트레이에 담는 모습 (세로) | `washing` | 공정 03 세척 |
| 3 | 하우스 건조장 내부, 선반에 널린 고추 (가로) | `drying-house` | 공정 04 건조, 갤러리 |
| 9 | 크레이트를 옮기며 작업하는 모습 (세로) | `sorting` | 공정 05 선별 |
| 5 | 건조대 위 고추 근접, 윤기 있는 붉은색 (세로) | `dried-pepper` | 상품 카드, 배너 배경, 갤러리 |
| 6 | 세척기와 건조 시설 전경 (가로) | `facility` | 갤러리 |
| 13 | 대표가 건조 트레이 앞에서 엄지를 든 모습 (가로) | `farmer-cover` | 농부 커버 (상하 크롭해 배너 비율로) |
| 14 | 대표 인물 사진 | `farmer-face` | 프로필 원형 (얼굴 중심 정사각으로 미리 크롭) |

### 사용하지 않는 파일

- **4** — 건조대 사진. 5번과 내용이 겹친다. 여유가 되면 갤러리 예비로만.
- **7** — 대표가 세척기에 고추를 넣는 모습. 13번 커버와 겹친다. 갤러리 예비.
- **11 (명함)** — **절대 사이트에 올리지 않는다.** 계좌번호가 적혀 있다.
  주소·전화번호·대표자명 확인용으로만 참고한다.
- **12 (고춧가루 홍보 이미지)** — **사이트에 사용하지 않는다.**
  배경의 고추와 고춧가루가 실제 갈골농원 사진이 아닌 합성 이미지다.
  담긴 정보(600g / 18,000원)만 상품 데이터에 텍스트로 반영한다.

### 없는 사진

포장, 출고(택배 상자), 고춧가루 실물, 사과.
해당 자리는 사선 패턴 박스 + "사진 준비 중"으로 두고,
나중에 파일만 넣으면 되도록 만든다.

사선 패턴:
`repeating-linear-gradient(45deg,#EFEAE1,#EFEAE1 9px,#E5DFD4 9px,#E5DFD4 18px)`

---

## 6. 섹션 순서

```
Header → Hero → 농부 → 숫자(딥그린) → 농원 소개
→ 재배 과정 → 원칙 → 상품 → 농장 사진 → 배너 → 오시는 길 → FAQ → Footer
+ 모바일 하단 고정바
```

---

## 7. Header (sticky, 높이 60px)

배경 `rgba(255,255,255,.93)` + backdrop-blur, 하단 1px 구분선.

- 좌측: `갈골농원`(19px, weight 800) + 옆에 작게 `경북 영양`(11.5px, --faint)
- 우측: `☎ 054-682-7178` (14.5px weight 700, --red, 배경 --red-soft, radius 9px, `tel:0546827178`)
- **메뉴는 두지 않는다.** 한 페이지짜리 사이트라 스크롤로 다 보인다.

---

## 8. Hero (사진 위 오버레이)

- 최소 높이 `clamp(540px,82vh,700px)` / PC `clamp(600px,88vh,760px)`, 콘텐츠 하단 정렬
- 배경 `hero-farm.jpg`, 오버레이
  `linear-gradient(180deg, rgba(18,24,16,.46) 0%, rgba(18,24,16,.10) 36%, rgba(13,19,11,.82) 100%)`
- eyebrow: `경상북도 영양군 청기면` (앞에 16px 짧은 가로선, 흰색 반투명)
- h1 (줄바꿈 그대로):

```
영양에서 직접 키우고
직접 말렸습니다.
```

- 보조 문구 (줄바꿈 그대로, 첫 줄 끝 마침표 없음):

```
국산 100% 영양고추
갈골농원이 재배부터 세척, 건조까지 직접 합니다.
```

- 버튼: `2026 햇고추 보기`(빨강, #products) / `갈골농원 이야기`(흰 테두리 고스트, #about)
  **모바일에서는 세로로 쌓고 폭을 꽉 채운다.** PC에서만 가로 배치.
- 하단 안내: `주문은 전화로 받고 있습니다 · 온라인 스토어 준비 중` (13.5px, 흰색 72%)

---

## 9. 농부 (커버 + 겹친 프로필)

```
[커버 사진 130px / PC 190px, 위쪽 모서리만 라운드]
[흰 카드 : 커버에 걸친 원형 프로필 88px / PC 110px, 흰 테두리 4px, margin-top -44px]
  갈골농원 대표          ← 12px, weight 700, letter-spacing .09em, --red
  김준연                ← 23px / PC 27px, weight 800
  경상북도 영양군 청기면   ← 12.5px, --faint

  수확한 고추를 세척기에 넣고, 건조기 온도를 맞추고, 상태를 살펴 고르는 일까지
  직접 합니다. 매년 다시 찾아주시는 분들이 믿고 드실 수 있는 고추를 보내는 것을
  가장 중요하게 생각합니다.
  ─────────────────────────
  054-682-7178  주문 · 문의   ← tel: 링크
```

- 섹션 eyebrow `FARMER`, 제목 `고추를 키운 사람.`
- 프로필 사진은 CSS 크롭에 맡기지 말고 **얼굴 중심 정사각으로 미리 잘라서** 넣는다.

---

## 10. 숫자 (딥그린 + 세로 원형)

- 배경 `--green`, 텍스트 흰색
- eyebrow `NUMBERS` (#A8C4AE), h2 `남의 손을 / 거치지 않습니다.`
- 설명: `밭에서 키우고, 씻고, 말리고, 골라 담아 보내는 일까지 영양군 청기면 농장에서 직접 합니다.`
- 원 4개: 모바일 세로 1열(간격 30px), **PC 가로 4열**
  - 원 130px / PC 156px, 배경 `--ivory`, **연결선 없음**
  - 원 안: 항목명 11.5px(#6D8574) + 숫자 33px/PC 38px(#22331F, weight 800, ls -.055em)
  - 단위(%, 단계, 가지, g)는 14px --red
  - 원 아래: 설명 13.5px, 흰색 68%

```
100 %    국산 비율   / 경북 영양산
7 단계    관리 공정   / 재배부터 출고까지
3 가지    취급 품목   / 고추 · 고춧가루 · 사과
600 g    햇고춧가루  / 한 봉 18,000원
```

숫자는 화면 진입 시 0부터 카운트업.

---

## 11. 농원 소개 (#about)

`.narrow` 사용. eyebrow `ABOUT`, h2 `직접 키운 고추만 / 보내드립니다.`

본문 2단락:

```
경상북도 영양군 청기면에서 고추 농사를 짓습니다. 밭에서 고추를 키우는 것부터
수확하고, 씻고, 말리는 과정까지 농장에서 직접 관리합니다.

중간 유통을 거치지 않고 농가에서 바로 보내드립니다. 누가 어디에서 어떻게
키웠는지 확인할 수 있는 고추입니다.
```

아래 정보표 (가로 구분선 4줄, `grid-template-columns: 82px 1fr` / PC `110px 1fr`).
항목명 12.5px weight 700 letter-spacing .06em --faint, 값 15.5px:

```
소재지      경상북도 영양군 청기면
취급 품목    고추 · 고춧가루 · 사과
건조 방식    세척 후 열풍건조
주문        전화 주문 054-682-7178
```

---

## 12. 재배 과정 (#process) — 사진 위 오버레이 격자

- 섹션 배경 `--soft`
- eyebrow `PROCESS`, h2 `밭에서 상자까지, / 일곱 단계.`
- 설명 `모든 단계를 농장에서 직접 관리합니다.`
- **모바일 2열 / PC 4열 격자, 카드는 정사각(`aspect-ratio: 1/1`), 간격 12px/18px, radius 14px**
- 각 카드: 사진이 전체를 채우고 그 위에 오버레이
  `linear-gradient(180deg, rgba(0,0,0,.10) 32%, rgba(0,0,0,.72) 100%)`
- 카드 하단 좌측에 텍스트:
  - meta 11.5px weight 700 (번호는 흰색 800, 나머지 흰색 78%)
  - 제목 16px / PC 17.5px, weight 800, `text-shadow: 0 1px 9px rgba(0,0,0,.45)`

```
01 재배   영양의 밭에서 키웁니다.        pepper-plant.jpg
02 수확   붉게 익은 것만 땁니다.         harvest.jpg
03 세척   말리기 전에 씻습니다.          washing.jpg   ← 라벨 [위생관리]
04 건조   열풍건조기로 말립니다.         drying-house.jpg
05 선별   하나씩 확인합니다.             sorting.jpg
06 포장   상하지 않게 담습니다.          사진 없음
07 출고   산지에서 바로 보냅니다.        사진 없음
08        (문구 카드)
```

- **03 세척**: meta 줄에 `<b>03</b> 세척 <span class="tag">위생관리</span>` 형태로 **같은 줄 인라인**.
  라벨은 흰 배경(rgba(255,255,255,.92))에 빨간 글씨. 별도 줄로 빼지 않는다.
- **06·07**: 사선 패턴 배경 + 좌측 상단에 `사진 준비 중` 배지(흰 배경 80%, --faint).
  오버레이는 더 옅게(52%).
- **08번 칸**: 단계가 7개라 격자에 빈칸이 생긴다. 딥그린(`--green`) 문구 카드로 채운다.

```
갈골농원                  ← 11px, letter-spacing .11em, #A8C4AE
일곱 단계 모두
농장에서 직접.
```

---

## 13. 원칙 (#rules)

eyebrow `원칙`, h2 `이렇게 농사짓습니다.`
가로 구분선 리스트 4개 (PC는 2열).
각 항목: `[번호(빨강 12.5px)] [키워드(--faint 12.5px)]` 한 줄 → 제목 18.5px → 설명 15.5px

```
01 국산 100%   경북 영양에서만.   직접 재배한 고추만 상품으로 씁니다.
02 직접 재배    밭부터 수확까지.   남에게 맡기지 않고 농장에서 직접 합니다.
03 위생관리     씻어서 말립니다.   세척기로 씻은 뒤 열풍건조기에 넣습니다.
04 산지 직송    중간 유통 없이.    농가에서 소비자에게 바로 보내드립니다.
```

---

## 14. 상품 (#products) — 쇼핑몰형 2열

- 섹션 배경 `--soft`
- eyebrow `PRODUCT`, h2 `2026년 햇고추.`
- 설명 `주문은 전화로 받고 있습니다. 온라인 스토어는 준비 중입니다.`
- **모바일 2열 / PC 4열**, 카드 radius 14px, 그림자 없음, 사진 `aspect-ratio: 1/1`
- 카드 내부 padding 12~13px (PC 16~17px):
  meta 11px → 상품명 15.5px/PC 17.5px → 설명 12.5px → 가격 17.5px/PC 20px → 버튼(높이 40px, 13.5px)

```
01  600g · 국내산   햇고춧가루    직접 재배해 씻어 말린 고추를 빻았습니다.
                                18,000원   [☎ 전화로 주문하기] (빨강)
                                사진 없음 → "고춧가루 사진 준비 중"

02  국내산          햇 건고추     세척 후 열풍건조한 통고추입니다.
                                가격 문의   [☎ 전화 문의] (테두리)
                                dried-pepper.jpg

03  국내산          영양 사과     수확 시기에 맞춰 안내드립니다.
                                가격 문의   [☎ 전화 문의] (테두리)
                                사진 없음 → "사과 사진 준비 중"

04  (문의 카드) 점선 테두리, 배경 없음
    찾는 상품이 없으신가요?
    필요한 수량이나 포장 단위를 전화로 말씀해 주세요.
    ☎ 054-682-7178
```

상품은 배열로 관리한다. `price: null` 이면 화면에 "가격 문의"로 표시되게 하고,
`available: false` 면 아예 렌더링하지 않게 만든다.

---

## 15. 농장 사진 (#gallery)

- eyebrow `농장`, h2 `영양에서, 이렇게.`
- 사진 3장, 모바일 세로 1열 / PC 3열. `aspect-ratio: 4/3`, radius 18px
- 각 사진 아래 캡션 13.5px --faint

```
dried-pepper.jpg   건조대에 널어 말리는 고추.
facility.jpg       농장 한쪽의 세척기와 건조 시설.
drying-house.jpg   하우스 건조장 내부.
```

---

## 16. 배너

- 배경 `dried-pepper.jpg` + `rgba(38,18,14,.62)` 오버레이, 가운데 정렬, 상하 86px
- eyebrow `갈골농원`, h2 `올해도, 같은 마음으로.`
- 설명 `영양에서 키운 고추를 산지에서 바로 보내드립니다.`
- 버튼 `☎ 054-682-7178` (빨강)

---

## 17. 오시는 길 (#visit)

eyebrow `VISIT`, h2 `영양군 청기면, / 산 아래 농원.`
흰 박스 2개 (모바일 세로 / PC 가로):

```
주소   경상북도 영양군 청기면 재일로 809-14
       갈골농원
       [네이버 지도 ↗]   ← siteConfig.naverMapUrl, target="_blank"

문의   054-682-7178
       주문·문의는 전화로 받고 있습니다
       [전화 걸기]   ← 빨강 버튼
```

---

## 18. FAQ (#faq)

섹션 배경 `--soft`, `.narrow`. eyebrow `FAQ`, h2 `자주 묻는 질문.`
`<details>` 아코디언, 우측에 회전하는 화살표(45deg → -135deg). 첫 항목만 open.

```
정말 국산 고추인가요?      갈골농원이 경상북도 영양군에서 직접 재배한 국내산 고추입니다.
어떻게 말리나요?           수확한 고추를 세척기로 씻은 뒤 열풍건조기에서 말립니다.
어디에서 보내나요?         경상북도 영양군 청기면 갈골농원에서 포장해 발송합니다.
주문은 어떻게 하나요?      현재는 전화로 주문을 받고 있습니다. 054-682-7178로 연락 주시면 안내해 드립니다.
올해 수확한 고추인가요?    해당 연도 햇고추 판매 여부는 상품 안내에 표시해 드립니다.
```

배송비·배송일·매운 정도·세척 여부 등 확인되지 않은 항목은 만들지 않는다.

---

## 19. Footer

배경 `--deep`, 글자 #9C948A, 상하 44px/40px.

```
갈골농원                        ← 17px weight 800 흰색
국산 100% 영양고추 · 고춧가루

대표 · 김준연
주소 · 경상북도 영양군 청기면 재일로 809-14
전화 · 054-682-7178            ← tel: 링크
─────────────────────
© 2026 갈골농원. 경북 영양 산지직송.
```

---

## 20. 모바일 하단 고정바

`position: fixed`, 배경 `rgba(255,255,255,.96)` + blur, 상단 1px 구분선,
`padding-bottom: calc(10px + env(safe-area-inset-bottom))`,
**768px 이상에서는 숨김.**

```
[상품 보기](테두리, #products)   [☎ 전화 주문](빨강, tel:)
```

---

## 21. 폴더 구조

```
app/
├ layout.tsx        메타데이터·OG·Pretendard
├ page.tsx
└ globals.css       CSS 변수만

components/
├ Header.tsx    ├ Hero.tsx      ├ Farmer.tsx    ├ Numbers.tsx
├ About.tsx     ├ Process.tsx   ├ Rules.tsx     ├ Products.tsx
├ Gallery.tsx   ├ Banner.tsx    ├ Visit.tsx     ├ Faq.tsx
├ Footer.tsx    └ MobileBar.tsx

data/
├ site.ts   ├ products.ts   ├ process.ts   ├ rules.ts   ├ faq.ts

public/images/
```

설정값은 한 곳에서만 고치게 만든다.

```ts
// data/site.ts
export const siteConfig = {
  name: "갈골농원",
  region: "경북 영양",
  owner: "김준연",
  slogan: "영양에서 직접 키우고, 직접 말렸습니다.",
  address: "경상북도 영양군 청기면 재일로 809-14",
  phone: "054-682-7178",
  phoneHref: "tel:0546827178",
  naverMapUrl: "https://map.naver.com/p/search/경상북도%20영양군%20청기면%20재일로%20809-14",
  smartStoreUrl: "",   // 개설 후 입력하면 구매 버튼이 전화 → 스토어로 전환되게
  // 계좌번호는 넣지 않는다 (전화 안내)
}
```

```ts
// data/products.ts
export const products = [
  { id:"gochugaru-600", name:"햇고춧가루", meta:"600g · 국내산",
    desc:"직접 재배해 씻어 말린 고추를 빻았습니다.",
    price:18000, image:null, available:true },
  { id:"dried-pepper", name:"햇 건고추", meta:"국내산",
    desc:"세척 후 열풍건조한 통고추입니다.",
    price:null, image:"/images/dried-pepper.webp", available:true },
  { id:"apple", name:"영양 사과", meta:"국내산",
    desc:"수확 시기에 맞춰 안내드립니다.",
    price:null, image:null, available:true },
]
```

---

## 22. SEO

- title: `갈골농원 | 국산 100% 영양고추 산지직송`
- description: `경북 영양에서 직접 재배하고 세척·건조한 국산 100% 영양고추. 갈골농원이 농장에서 직접 보내드립니다.`
- 키워드: 영양고추 / 영양 건고추 / 국산 고춧가루 / 경북 영양 고추 / 햇고추 / 갈골농원 / 고추 산지직송
- Open Graph 설정 (카카오톡 공유 시 대표 이미지 = hero-farm)
- 이미지 alt 텍스트를 모두 채운다

---

## 23. 성능·접근성

- next/image 적극 활용, WebP, lazy loading (Hero만 priority)
- 중장년 사용자가 많다. **본문을 16.5px 이하로 줄이지 않는다.**
- 버튼 터치 영역 최소 44px
- 색 대비 확보, 어려운 영어 표현 금지
- 버튼 문구는 목적을 명확히: `Shop`(X) → `2026 햇고추 보기`(O)

---

## 24. 작업 순서

1. `galgol-preview-mobile.html` 을 열어 구조와 스타일을 파악한다.
2. Next.js 프로젝트를 생성하고 Tailwind 설정에 위 색상 토큰을 등록한다.
3. `photos/` 사진을 5번 대응표대로 이름을 바꿔 `public/images/` 로 옮기고 WebP로 변환한다.
   11번(명함)과 12번(홍보 이미지)은 옮기지 않는다.
4. `data/` 파일부터 만든다.
5. 컴포넌트를 섹션 순서대로 구현한다. **모바일 폭에서 먼저 완성하고** PC 대응을 얹는다.
6. 모바일(390px)·태블릿(768px)·PC(1280px) 세 폭에서 확인한다.
7. GitHub 저장 → Vercel 연결 → Push 시 자동 재배포되게 구성한다.
8. 도메인은 추후 Vercel에 연결할 수 있게 열어둔다.

---

## 25. 우선순위

```
1순위  실제 농가라는 신뢰감
2순위  사진의 품질과 활용
3순위  모바일 사용성
4순위  전화 문의 전환
5순위  유지보수가 쉬운 코드
```
