@AGENTS.md

# Deployment workflow

This site auto-deploys from `master` via Vercel. Whenever a code change is
finished and verified, merge/push it straight to `master` (fast-forward
when possible) instead of leaving it stranded on a feature branch — the
user wants changes live without a separate merge step or PR review.
